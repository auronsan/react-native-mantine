import { Space } from './index';

describe('Space', () => {
  it('exports Space component', () => {
    expect(Space).toBeDefined();
  });

  it.todo('add tests when testing-library is configured');
});
