import { Center } from './index';

describe('Center', () => {
  it('exports Center component', () => {
    expect(Center).toBeDefined();
  });

  it.todo('add tests when testing-library is configured');
});
