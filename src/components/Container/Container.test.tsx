import { Container } from './index';

describe('Container', () => {
  it('exports Container component', () => {
    expect(Container).toBeDefined();
  });

  it.todo('add tests when testing-library is configured');
});
