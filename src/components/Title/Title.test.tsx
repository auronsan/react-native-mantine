import { Title } from './index';

describe('Title', () => {
  it('exports Title component', () => {
    expect(Title).toBeDefined();
  });

  it.todo('add tests when testing-library is configured');
});
