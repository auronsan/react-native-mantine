# Snack starter

`App.js` is the demo behind the "Try it in Expo Snack" link in the main README.
Snack no longer imports directly from GitHub, so the link embeds this file.
After editing `App.js` or `package.json`, regenerate the link:

```bash
node scripts/snack-url.mjs
```

and paste the output into the README.
